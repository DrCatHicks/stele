# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# dbt
target/
dbt_packages/
logs/
.user.yml

# Per-run dbt artifact archive written by scripts/run_etl.py (§3.7). The
# ops.etl_runs row is the index; the artifacts themselves aren't checked in.
dbt/etl_artifacts/

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
.idea/

# Claude Code — personal/local settings (shared settings.json IS committed)
.claude/settings.local.json
# Local git worktrees created by Claude Code agents (not project content)
.claude/worktrees/

# Node
node_modules/
dist/
.pnpm-store/
*.tsbuildinfo

# Misc
*.log
