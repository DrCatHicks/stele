name: survey_engine
version: '0.0.0'
config-version: 2

profile: survey_engine

model-paths: ['models']
seed-paths: ['seeds']
test-paths: ['tests']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

target-path: target
clean-targets: [target, dbt_packages]

models:
  survey_engine:
    staging:
      +schema: stg
      +materialized: view
    intermediate:
      +schema: stg
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
